# 个人主页部署和自定义指南

这个指南将帮助你把这个项目修改成你自己的个人主页，并部署到 GitHub Pages 上。

## 目录
1. [准备工作](#准备工作)
2. [修改成自己的内容](#修改成自己的内容)
3. [配置访问者地图](#配置访问者地图)
4. [本地预览](#本地预览)
5. [部署到 GitHub Pages](#部署到-github-pages)

---

## 准备工作

### 1. 注册 GitHub 账号
如果你还没有 GitHub 账号，请先访问 [github.com](https://github.com) 注册一个。

### 2. 安装 Git
- Windows: 下载并安装 [Git for Windows](https://git-scm.com/download/win)
- Mac: 运行 `brew install git`
- Linux: 运行 `sudo apt-get install git`

### 3. 安装 Ruby 和 Jekyll（可选，用于本地预览）
如果你想在本地预览网站，需要安装 Jekyll：
- Windows: 使用 [RubyInstaller](https://rubyinstaller.org/) 安装 Ruby，然后运行 `gem install bundler jekyll`
- Mac: 运行 `brew install ruby`，然后 `gem install bundler jekyll`
- Linux: 运行 `sudo apt-get install ruby-full build-essential`，然后 `gem install bundler jekyll`

---

## 修改成自己的内容

### 1. 修改 `_config.yml` 文件
这是最重要的配置文件，打开 `_config.yml`，修改以下内容：

```yaml
# Site Settings
title                    : "你的名字"
description              : "你的简介，比如：PhD at XXX University"
direction                : "你的研究方向或个人描述"
repository               : "你的github用户名/你的github用户名.github.io"

# Site Author
author:
  name          : "你的名字"
  avatar        : "images/你的头像文件名.jpg"  # 把你的头像放到 images 文件夹
  email         : "你的邮箱@example.com"
  googlescholar : "你的Google Scholar链接"
  github        : "你的GitHub用户名"
  twitter       : "你的Twitter用户名"
  linkedin      : "你的LinkedIn用户名"
```

### 2. 更换头像
- 把你的头像图片放到 `images/` 文件夹中
- 在 `_config.yml` 中修改 `avatar` 字段指向你的头像文件

### 3. 修改个人介绍页面
打开 `_pages/about.md`，修改成你自己的内容。

### 4. 修改其他内容
- 查看 `_data/` 文件夹下的文件，修改成自己的信息
- 查看 `_pages/` 文件夹下的其他页面，根据需要修改

---

## 配置访问者地图

我们已经在页面中添加了 ClustrMaps 访问者地图组件，你需要获取自己的地图 ID：

### 步骤：
1. 访问 [ClustrMaps.com](https://clustrmaps.com)
2. 点击 "Get Your Map" 或 "Free Sign Up"
3. 输入你将要部署的网站地址（格式：`https://你的用户名.github.io`）
4. 注册账号（免费）
5. 注册成功后，你会获得一段代码，类似这样：
   ```html
   <script type="text/javascript" id="clustrmaps" src="//clustrmaps.com/map_v2.js?d=你的ID参数&w=240&h=240&co=faf9f7&ct=3d3929"></script>
   ```
   或者是一个图片链接：
   ```
   https://www.clustrmaps.com/map_v2.png?d=这里是你的ID&w=240
   ```

6. 找到 `d=` 后面的那一串字符，这就是你的 ID
7. 打开 `_includes/author-profile.html` 文件
8. 找到这一行：
   ```html
   <img id="clustrmap-img" src="https://www.clustrmaps.com/map_v2.png?cl=c2714f&w=240&t=tt&d=REPLACE_WITH_YOUR_ID&co=faf9f7&ct=3d3929" alt="Visitor Map" />
   ```
9. 把 `REPLACE_WITH_YOUR_ID` 替换成你从 ClustrMaps 获取的 ID

---

## 本地预览

如果你安装了 Jekyll，可以在本地预览网站：

1. 在项目根目录打开终端/命令行
2. 运行以下命令：
   ```bash
   bundle install
   bundle exec jekyll serve
   ```
3. 打开浏览器访问 `http://localhost:4000` 查看效果

如果没有安装 Jekyll，也可以直接部署到 GitHub 后在线查看。

---

## 部署到 GitHub Pages

### 步骤 1：创建 GitHub 仓库
1. 登录 GitHub
2. 点击右上角的 "+" 号，选择 "New repository"
3. **重要**：仓库名称必须是 `你的用户名.github.io`（例如：如果你的用户名是 `zhangsan`，仓库名就是 `zhangsan.github.io`）
4. 选择 "Public"
5. 点击 "Create repository"

### 步骤 2：上传代码

#### 方法一：使用 Git 命令行（推荐）

1. 在项目根目录（`my-personal-website` 文件夹）打开终端/命令行
2. 运行以下命令（替换成你的信息）：

```bash
# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交更改
git commit -m "Initial commit"

# 关联到你的 GitHub 仓库（替换成你的用户名）
git remote add origin https://github.com/你的用户名/你的用户名.github.io.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

#### 方法二：使用 GitHub 网页界面上传

1. 在你创建的仓库页面，点击 "uploading an existing file"
2. 把 `my-personal-website` 文件夹中的所有文件拖拽到上传区域
3. 填写提交信息，点击 "Commit changes"

### 步骤 3：启用 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 "Settings"（设置）
3. 在左侧菜单中找到 "Pages"（位于 "Code and automation" 部分）
4. 在 "Build and deployment" 下：
   - Source 选择 `Deploy from a branch`
   - Branch 选择 `main`（或 `master`）
   - 文件夹选择 `/ (root)`
5. 点击 "Save"

### 步骤 4：等待部署完成

- GitHub Pages 通常需要 1-5 分钟来部署
- 部署完成后，你可以在 `https://你的用户名.github.io` 访问你的网站
- 你可以在仓库的 "Actions" 标签页查看部署状态

---

## 后续更新

当你修改了内容后，只需要运行以下命令更新：

```bash
git add .
git commit -m "描述你的修改"
git push
```

GitHub Pages 会自动重新部署。

---

## 常见问题

**Q: 我的网站没有显示？**
- 确保仓库名称正确：`你的用户名.github.io`
- 等待几分钟，首次部署需要时间
- 检查 Pages 设置是否正确

**Q: 地图没有显示？**
- 确保你已经替换了正确的 ClustrMaps ID
- 确保你的网站已经上线（ClustrMaps 需要能访问到你的网站）
- 可以尝试重新注册一个 ClustrMaps 账号

**Q: 如何修改配色和样式？**
- 修改 `_sass/` 文件夹下的 `.scss` 文件
- 主要颜色变量在 `_sass/_variables.scss` 中

---

## 下一步

- 访问你的网站：`https://你的用户名.github.io`
- 把网站链接分享给朋友！
- 持续更新你的个人主页内容

祝你拥有一个漂亮的个人主页！🎉
